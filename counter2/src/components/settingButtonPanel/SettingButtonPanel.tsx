import {Button} from '../buttonPanel/Button.tsx';

export const SettingButtonPanel = () => {

    return (
        <div className="buttonPanel">
            <Button title="set" onClick={() => {
            }}/>
        </div>
    )
}